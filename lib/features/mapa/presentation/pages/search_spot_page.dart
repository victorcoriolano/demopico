import 'package:flutter/material.dart';

class SearchSpotPage extends StatefulWidget {
  const SearchSpotPage({super.key});

  @override
  State<SearchSpotPage> createState() => _SearchSpotPageState();
}

class _SearchSpotPageState extends State<SearchSpotPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Placeholder(),
    );
  }
}